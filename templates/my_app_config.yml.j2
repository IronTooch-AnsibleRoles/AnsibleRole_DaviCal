#
# Note: After making changes to this file you need to run the `rebuild` or `update`
# command for them to be applied.

url: https://{{ davical_external_domain }}
http_port: 80
https_port: 443
ssl_certificate_path: {{ davical_certificate_path }}
ssl_key_path: {{ davical_key_path }}}

yaml_list:
{% for internal_yaml_item in davical_list_data %}
  - {{ internal_yaml_item }}
{% endfor %}
