galaxy_info:
  role_name: my_role
  author: IronTooch
  description: App_Description
  license: GPL-3.0-only
  min_ansible_version: "2.1"

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags: []

dependencies:
  - role: irontooch.postgresql
    vars:
      postgres_app_name: "{{ davical_name }}"
